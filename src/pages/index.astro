---
// import { Image } from "@astrojs/image/components";
import BaseLayout from '../layouts/BaseLayout.astro';
import MarketList from '../components/MarketList.astro';
// import { generateItemForId } from "../utils.js";

// import rankedNfts from "./rarest/first.json";

// export async function getStaticPaths() {
//   const rankedNfts = (await import("./rarest/first.json")).default;
//   // console.log(res);
//   return [{ props: { nfts: rankedNfts } }];
// }

// const nfts = await fetch('/rarest/first.json').then((res) => res.json());
// const res = (await import("./rarest/first.json")).default;
// console.log(res);

// const nfts = (await import("./rarest/first.json")).default.slice(0, 50);
---

<BaseLayout>
  <div class="container my-12 px-6 mx-auto">
    <section class="mb-32 text-gray-800">
      <h2 class="text-6xl font-extrabold mb-12 text-center">
        🌲 <em
          class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 to-green-500"
          >Ether Logs</em
        > 🪵<span class="animate-pulse">🔥</span>
      </h2>

      <div
        class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6 gap-4"
      >
        <MarketList />
      </div>
    </section>
  </div>
</BaseLayout>
